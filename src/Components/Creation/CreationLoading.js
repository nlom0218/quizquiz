import React from 'react';
import { connect } from 'react-redux';

const CreationLoading = () => {
    return (<>
        <div>Loading 하는거 구현하기!!</div>
    </>);
}

export default connect()(CreationLoading);